db:
  name: webgate_pro_test

# Keep this key present so RSpec verified partial doubles can stub it safely.
# Tests must never call external GPT APIs.
services:
  gpt:
    enabled: false
    key: ''

# Backward-compatible key (deprecated)
gpt_key: ''
